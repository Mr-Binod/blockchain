{
  // compilerOptions 코드 변환시 사용하는 속성들들
  "compilerOptions": {
    "target": "es6", // 출력할 js 의 문법 대상
    "module": "commonjs", // 사용하는 모듈 시스템
    "strict": true,  // 엄격한 타입 검사
    "outDir": "./dist",  // where to send the transfiled files
    "sourceMap": true,  // 디버깅 소스를만들어서 어디서 에러가 발생했는지   .js.map   typescript에서 에러를 추석할수 있게 한다
  },
  "include": ["src/**/*"] // include compile file path  /** src all folders /* src all folders files
}
